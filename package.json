{
  "name": "onestarstream-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages/onestar-db-sdk && npm run build --workspace=apps/desktop",
    "build:sdk": "npm run build --workspace=packages/onestar-db-sdk",
    "build:app": "npm run build --workspace=apps/desktop",
    "dev": "npm run dev --workspace=apps/desktop",
    "dev:electron": "npm run dev:electron --workspace=apps/desktop",
    "test:e2e": "npm run test:e2e --workspace=apps/desktop",
    "clean": "rm -rf packages/*/dist apps/desktop/.next apps/desktop/electron/dist node_modules packages/*/node_modules apps/desktop/node_modules"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
